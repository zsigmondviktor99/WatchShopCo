@model List<Brand>;

@{
    ViewData["Title"] = "F.A.Q.";
}

<div class="row">
    <div class="col-12">
        <h1>Brands</h1>
    </div>

    @{           
        foreach (Brand brand in Model)
        {
            <div class="col-12 col-sm-6 col-md-4">
                    <h3>
                        @brand.Name
                    </h3>
            </div>
        }
    }
</div>
